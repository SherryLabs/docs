---
title: "Action Kit"
description: "On-chain blockchain actions embedded in social — action builders, token management, directory listing, indexing, and rewards"
sidebarTitle: "Overview"
---

The Action Kit lets you build and deploy **blockchain actions**: on-chain actions (swaps, transfers, bridges) that can be embedded directly inside any platform. Users discover blockchain actions through the directory, execute them in-context, and earn rewards. Operators register blockchain actions, configure actions via builders, and track engagement through indexing and rewards endpoints.

## Core Concepts

### Blockchain Actions

A blockchain action is an on-chain action packaged with metadata so it can be rendered and executed in any context. Each blockchain action has:

- A **type** — `swap`, `onboarding`, etc.
- **Metadata** — the action definition (chains, tokens, amounts, UI config)
- A **registry entry** — your operator's record for the blockchain action

Blockchain actions are created via `POST /v1/action` and their metadata is generated using the Action Builders.

### Action Builders

Action Builders generate the structured metadata that defines what a blockchain action does. Instead of constructing raw JSON, you call a builder endpoint with high-level parameters:

| Builder | Endpoint | Purpose |
|---------|----------|---------|
| Swap | `POST /v1/action/builders/swap` | Token swap on a single chain |
| Staking | `POST /v1/action/builders/staking` | Stake tokens in a protocol |
| Crosschain Transfer | `POST /v1/action/builders/crosschain-transfer` | Send tokens across chains |
| Native Transfer | `POST /v1/action/builders/transfer-native` | Send native tokens (ETH, MATIC) |
| Crosschain Bridge | `POST /v1/action/builders/crosschain-bridge` | Bridge assets between networks |

The builder returns a metadata object you store when creating or updating a blockchain action.

### Token Management

The tokens endpoint provides the supported token list for any chain and protocol combination. Use this to populate token selectors in your blockchain action UI before calling a builder.

```
GET /v1/action/tokens/:chain/:protocol
```

Example: `GET /v1/action/tokens/polygon/uniswap` returns all Uniswap tokens on Polygon.

### Directory

The directory is a public listing of all published blockchain actions across operators. Embedders query the directory to discover available actions for their platform.

```
GET /v1/action/directory
```

No authentication required for directory reads.

### Indexing

When a user executes a blockchain action, your platform reports the on-chain event to Relayer via the indexing endpoint. This powers rewards tracking and analytics.

```
POST /v1/action/indexing/events
```

### Rewards & Referrals

The rewards system tracks KOL (Key Opinion Leader) performance metrics: which blockchain actions they've shared, how many on-chain executions resulted from their referrals, and aggregated volume. Metrics are queryable by wallet address.

```
GET /v1/action/rewards/metrics?address=0xYourAddress
GET /v1/action/rewards/blockchain actions-with-onchain-metrics?address=0xYourAddress
```

## Blockchain Action Lifecycle

```mermaid
graph LR
    A[Choose Action Type] --> B[Generate Builder Metadata]
    B --> C[Create Blockchain Action in Registry]
    C --> D[Published in Directory]
    D --> E[User Executes On-Chain]
    E --> F[Index Event]
    F --> G[Rewards Tracked]
```

1. **Design** — Choose an action type and get token list for your chain + protocol
2. **Build** — Call a builder to generate the action metadata
3. **Register** — Create the blockchain action in the registry with the builder metadata
4. **Discover** — Blockchain action appears in the directory for embedders
5. **Execute** — Users interact with the blockchain action on any platform
6. **Track** — Index on-chain events; query rewards metrics for KOLs

## Endpoint Summary

| Group | Prefix | Purpose |
|-------|--------|---------|
| Registry | `/v1/action` | Create and manage blockchain actions |
| Builders | `/v1/action/builders` | Generate action metadata |
| Directory | `/v1/action/directory` | Public blockchain action discovery |
| Tokens | `/v1/action/tokens` | Supported token lists |
| Indexing | `/v1/action/indexing` | Report on-chain execution events |
| Rewards | `/v1/action/rewards` | KOL metrics and referral tracking |

## Next Steps

<CardGroup cols={2}>
  <Card title="Flow Guide" icon="route" href="/action/flow-guide">
    Build and deploy a blockchain action step by step.
  </Card>
  <Card title="Endpoints" icon="code" href="/action/endpoints">
    Full reference for builders, registry, and rewards.
  </Card>
</CardGroup>
